require 'data_mapper'

DataMapper::Logger.new($stdout, :debug)
DataMapper.setup(:default, {:adapter => 'postgres',
                            :host => 'localhost',
                            :database => '/lunch',
                            :username => 'isaac',
                            :password => 'REDACTED'})
